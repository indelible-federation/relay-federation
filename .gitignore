node_modules/
.env
*.log
dist/
.DS_Store
Thumbs.db
coverage/
.nyc_output/
*.tgz

# Indelible-private files — must NEVER be committed to public repo.
# These belong in c:/Indelible-main/bridge/ (private repo) only.
# Deleted from public dir 2026-05-03 after April 30 separation cleanup.
packages/bridge/heartbeat.mjs
packages/bridge/telegram-bot.mjs
packages/bridge/replay-to-arc.js
packages/bridge/briefing.md

# Pull-from-live snapshots used during pull-live-first deploy discipline.
# These are scratch files, not source.
packages/bridge/cli.js.*-live
packages/bridge/cli.js.alpha-good
packages/bridge/cli.js.*.bak.*

# --- 2026-06-16 stray-file triage — keep Indelible/private out of the public repo ---
# Private fleet-ops docs (leak fleet IPs / mesh / repo-split glue). Moved to
# c:/indelible-docs/relay-fleet-ops/; patterns kept as a backstop against re-drops.
BRIDGE_RECOVERY_PROCEDURES.md
bridgesecurity414.md
SERVER_RESTART_HANDBOOK.md
P2P_PEER_RETENTION_PLAN.md
dashboard-v4-3d-backup.html
codex-*.md
plans/
packages/seed/

# Local-only working docs — kept in this clone, never pushed.
CRAWLER_HANDBOOK.md
relay-bridge.service
SPV_MEMPOOL_PLAN.md
TERANODE_INTEGRATION_HANDBOOK.md
arc-vs-federation-article.md
PR1-review.md
resume-zcool.md

# Junk / scratch / binaries — never belong in the repo.
*.exe
*.zip
*-backup.html
tmp-*
/art/        # root scratch art (15MB screenshots) — NOT packages/bridge/dashboard/art/ (legit dashboard assets)
